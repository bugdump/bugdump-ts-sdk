// Chunk entry point — built as a standalone file served next to the script tag, and never
// reachable from the SDK's static import graph. See lib/core/chunk-loader.ts.
export { default as html2canvas } from 'html2canvas-pro';
